name: "Catgirl Edition Extreme (znver4 + sched-ext)"

on:
  workflow_dispatch:
  push:
    branches: [ master ]

jobs:
  build:
    name: Build and Package
    runs-on: ubuntu-latest
    container:
      image: archlinux:latest
      options: --privileged

    steps:
      - name: Maximize build space
        run: |
          rm -rf /usr/share/dotnet
          rm -rf /usr/local/lib/android
          rm -rf /opt/ghc

      - name: Install dependencies
        run: |
          pacman -Syu --noconfirm
          pacman -S --noconfirm base-devel git zip bc cpio pahole python clang llvm lld sudo ncurses

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build linux-catgirl-edition
        run: |
          # Create build user
          useradd -m builduser
          chown -R builduser:builduser .
          printf 'builduser ALL=(ALL) NOPASSWD: ALL' > /etc/sudoers.d/builduser
          
          export WORKSPACE_PATH=$(pwd)          
          sudo -E -u builduser bash <<'EOF'
            set -e
            cd "$WORKSPACE_PATH"
            
            # --- THE NUCLEAR MENU KILLER ---
            # This forces the PKGBUILD to skip ALL interactive menus
            sed -i 's|_micro_optimize:=[^}]*}|_micro_optimize:=no}|' PKGBUILD
            sed -i 's|_using_nconfig:=[^}]*}|_using_nconfig:=no}|' PKGBUILD
            sed -i 's|_using_menuconfig:=[^}]*}|_using_menuconfig:=no}|' PKGBUILD
            sed -i 's|interactive=.*|interactive=no|' PKGBUILD
            
            # --- KERNEL CONFIG ---
            sed -i 's|_processor_opt:=[^}]*}|_processor_opt:=znver4}|' PKGBUILD
            sed -i 's|_cflags_O3:=[^}]*}|_cflags_O3:=yes}|' PKGBUILD
            sed -i 's|_use_llvm_lto:=[^}]*}|_use_llvm_lto:=thin}|' PKGBUILD
            sed -i 's|_cpusched:=[^}]*}|_cpusched:=bore}|' PKGBUILD
            
            # --- BYPASS CONFLICTS ---
            sed -i 's|_die "|true; #|g' PKGBUILD

            # --- PRE-CONFIGURING SCHED-EXT ---
            # We inject the config commands directly into the PKGBUILD prepare function
            sed -i '/prepare() {/a \
              KDIR=$(find . -maxdepth 1 -type d -name "linux-*" | head -n 1) \
              cd "$KDIR" \
              scripts/config -e BPF \
              scripts/config -e BPF_SYSCALL \
              scripts/config -e BPF_JIT \
              scripts/config -e DEBUG_INFO_BTF \
              scripts/config -e SCHED_CLASS_EXT \
              scripts/config --set-str DEFAULT_HOSTNAME "catgirl" \
              cd ..' PKGBUILD

            # Build it. The yes "" | ensures any hidden prompts get an 'Enter' key.
            export TERM=dumb
            yes "" | makepkg -scf --noconfirm --skipchecksums
          EOF

      - name: Prepare and Upload Artifacts
        run: |
          zip linux-catgirl-extreme-znver4.zip *.pkg.tar.zst
          
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: linux-catgirl-extreme-znver4
          path: linux-catgirl-extreme-znver4.zip
